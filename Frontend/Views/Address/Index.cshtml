@model List<AddressViewModel>



<section class="address-page">
    <div class="container">
        @if (Model != null)
        {
            <div class="address-list">
                @foreach (var address in Model)
                {
                    <a class="address" asp-action="AddressDetails" asp-controller="Address" asp-route-id="@address.Id">
                        <div class="address-icon">
                            <i class="fa-sharp fa-light fa-address-book"></i>
                        </div>
                        <div class="content">
                            <h5>@address.AddressLine1</h5>
                            @address.AddressLine1, @address.City
                        </div>
                        <div class="edit-icon">
                            <i class="fa-light fa-pen-line"></i>
                        </div>
                        
                    </a>
                }
            </div>
        }
        <div class="add-address">
            <a class="add-button" asp-action="AddAddress" asp-controller="Address">
                <i class="fa-light fa-plus"></i>
            </a>
            Add a new address
        </div>
    </div>
</section>